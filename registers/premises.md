# Premises — what the work is standing on

*One premise per row. **A premise is a claim being built on, not a question waiting for an answer** — that is what
separates this file from `questions.md`. A question blocks; a premise is assumed true and carries weight until
somebody breaks it. **IDs are not recycled, but dead rows are deleted** — a broken premise stays only until its
consequence has been applied.*

**Why this file exists** (Neil, 2026-08-18): the C-ATS run rested on a premise that was wrong — *nobody can size a
C-ATS room without ringing us* — and it was wrong for sixteen days inside a product record before anyone was
asked. It was never stated as a premise, so it was never available to be broken. It surfaced only because Neil
happened to read one line of a 640-line output. **A premise buried inside a deliverable can only be caught by
reading the deliverable. A premise in a row can be broken in seconds.**

## How it runs

- **Stated before the work, not after it.** Anything load-bearing goes in a row before it carries anything.
- **Source, in the unflattering categories** (`../method.md`): **Platform** (an ADR — authoritative) · **Engine**
  (read from the live system) · **Observed** (the accounts or the field) · **Repo** (this repo says so, which is
  weaker) · **Proposed here** (mine — you are ratifying a suggestion, not confirming a fact) · **Unknown**.
- **Every row names what would break it.** A premise with no falsifier is not a premise, it is a belief.
- **Status:** `holding` · `challenged` (evidence against it, not yet resolved) · `qualified` (true within stated
  limits) · `broken` (with the date and what broke it — **kept**, because the work built on it has to be findable
  and because a broken row is what stops the same assumption being made again).
- **A broken premise is not a failure, it is the point.** The register earns its place by how often rows break.

## The register

| # | The premise | Source | What rests on it | What would break it | Status |
|---|---|---|---|---|---|
| PR-1 | The brand's material is in `brands/<brand>/` | Proposed here | The whole brand-run method; every "missing" verdict in the three C-ATS pathways | Finding the answer in another repo | **broken 2026-08-18** — the C-ATS sizing method is systemised in `cinema-platform` `products/cinema-tools/`. Fix folded into `../brands/_template/the-brand-run.md` |
| PR-2 | What C-ATS is short of is **content** | Repo (`../group-strategy/strategy.md`) | The entire run; `../brands/c-ats/content-plan.md`; the case for a production line | Counting the blocked items and finding most are not authoring jobs | **challenged** — see the count below |
| PR-3 | The dealer's own words for each problem must come from the sent-mail archive (CON-3) and may not be drafted | Repo → Proposed here | The `[?]` at the head of all three pathways; every "problem named" hook's wording; `O1` on all three records | An owner simply saying the words — which costs minutes against days of mining | **broken 2026-08-18.** Neil: *"all of that shit about real recent jobs etc — what trash and bullshit. Just needs cleaned out regardless."* **The parking was ceremony, not caution.** `W2`'s *who* was answered in one sentence the first time it was asked (Q55). Cleaned out of `../brands/c-ats/` and `../brands/_template/`; still present in DT and Fabric Walls |
| PR-4 | The BSRIA measured data is C-ATS's strongest proof asset | Repo (`../brands/c-ats/positioning.md`) | Slot 4 on all three pathways; the proof-first ordering of the content plan | A stronger asset existing — and the design rules look like one | **broken 2026-08-18** (Q61). Neil: the credibility is *"the systemised method — that's the real answer."* **The data stays real, published and free; it is not what credibility rests on.** Consequences: slot 4 on all three pathways leads with the wrong asset, and **C-ATS's credibility claim is currently unpublishable** — the method is black-boxed, which makes DR-Q52 this brand's central question rather than a content one. **↻ RE-BROKEN 2026-08-19, and this time against me.** *Within a day, three pieces of work drifted back over this break — the `A1` axis reading, `Leg 4`'s first rule, and the acoustics scan's headline. Neil: **"it's too tempting to scream and point at measured data. Truthfully if it's not legal or safety no one gives a shit. Hardly any dealer cares about Sabines or anything. All the others are the same — confirm it's not just made up, but don't go mad that you have some tests either."*** **So the premise does not merely break, it inverts: measurement's only job is to show a claim is not invented.** *The single exception is a **compliance gate** — Class 0 / EN 13501-1 / IMO-SOLAS — which a specified project cannot complete without, and which travels with the record's own caveat: confirm the certificate for the exact product and finish.* **This row now carries the caution as well as the break, because the break on its own did not hold for a day.** |
| PR-5 | The consolidated ledger describes C-ATS's addressable field | Observed | `../brands/c-ats/segments.md`; the 53-of-549 finding; every segment count | The missing sources landing | **qualified** — Shopify (MON-8) covers the 2023→2026 middle and is absent; engine is not extracted; account identity is normalised to a floor, not to exactness |
| PR-6 | A dealer who already buys from the group re-enters C-ATS at stage 2, not stage 1 | Repo (`../motion/motion-design.md`) | The 142-account reading in `../brands/c-ats/segments.md` | Evidence that group trust does not transfer across brands | **qualified** by Q51 — it says what a dealer has *seen*, and must not become a ranking |
| PR-7 | Three problems means three doors | Repo (`../group-strategy/buyer-journey.md`) | Three pathway files; the hook set's structure | Evidence that a dealer does not arrive with a recognisable symptom | **BROKEN 2026-08-19 — on its own stated falsifier, at the third attempt.** *`PR-14` **is** that evidence: the specification happens at design stage, before the room exists, so there is no symptom to arrive with. **The count of three survives** (Neil, `Q71`: *"three stand"*); **what the doors are keyed to does not** — they are keyed to the three project moments `M1`–`M3` (`../brands/c-ats/claims.md` `C2.24`–`C2.26`).* **Worth keeping the sequence, because it is instructive:** I broke this row on 2026-08-19 off two words I had invented (*"the way in"*, *"the room"*), withdrew the break correctly when Neil asked what they meant, **and then it broke anyway an hour later on a premise he supplied.** *The withdrawal was right — I had defended `PR-7` on the anti-ranking point, which is `Q50`'s and is untouched. **A premise can be wrong and a challenge to it still be malformed**, and the two facts are unrelated.* Applied in `../brands/c-ats/pathways.md` |
| PR-8 | Hooks cannot be cast without an owner authoring the wording | Repo (S8 — authorship is not scaled) | Nothing in `../brands/c-ats/hooks.md` being castable today; the whole set waiting on Neil | An owner correcting a draft rather than authoring from blank — which is what the production line says everywhere else (`../motion/motion-design.md` component 2, step 3) | **BROKEN 2026-08-19, by acting on it.** *`hooks.md` now states that **every candidate is a draft offered for correction**, which is what `motion-design.md` says for every other deliverable. **Hooks were the only thing held to "author from blank", and nobody had noticed the inconsistency** — including me, when I recorded this row as merely `challenged` and left it.* **`PR-3` was the same shape** (an apparatus of parking that cost days and answered in one sentence), and this is the third time the fix has been *ask, or draft and let it be corrected*. **A struck candidate is as useful as an approved one** |
| PR-10 | Checking the platform means reading its documents | Proposed here | The 2026-08-18 correction to PR-1; DR-Q52's first draft; the "Tier-0 route exists" claim in three files | A document in the platform being built on a superseded ADR | **broken 2026-08-18, hours after PR-1** — `03-c-ats-partner-tool.md` (24 May) is framed on ADR 019, superseded by 017 on 13 Aug, and describes a Level 2 surface 017 v2 §5 withdrew. **Checking the platform is not enough; the ADR status is the check** |
| PR-11 | `positioning.md` states settled brand truth | Repo | Every hook's justification; all three pathways' propositions; `content-plan.md`; the whole C-ATS run | Checking who confirmed it | **broken 2026-08-18** — **zero attributions to Neil in 249 lines.** Drafted by a session, never confirmed, cited as settled by everything since. Atomising §1 gave 16 claims, of which 5 were `product-records.md` verbatim and 2 were open questions nobody had asked. Fix: `../brands/c-ats/claims.md` and `../method.md` § The drill-down |
| PR-24 | **This repo's notes about what is and is not published are current** — so a record field saying *"never stated"*, *"nowhere in the marketing"* or *"the gap is publication"* can be relied on | Repo — `product-records.md`'s `N3` status column, `content-plan.md`'s tranches, and the whole *"exists — unpublished"* vocabulary | **`KNW-1`** and `draft-answers.md` (six answers re-derived) · the **`T5`** training draft, built on *"nowhere in the marketing"* · `SIT-1`, a defect report against a caveat that was already live · `DOC-4`'s block on explaining the low figures | Fetching the generated site once | **broken 2026-08-20 — on the observation, not on the explanation.** *It breaks because **the answers are published and the record says they are not.** **The cause is not established**: Neil said the site was auto-generated from answers in this repo, then, the same day — ***"I'm sure that our other sites were used as reference also."*** **So provenance is mixed and the split is unknown** (`SIT-8`); my first version of this row asserted the record's own output as the source. *Either explanation breaks the premise, and **the older-site explanation breaks it harder**: it would mean the answers were public before this repo drafted them.* *`/technical-information/` already publishes: bond-or-screw with its reason, the **~300 mm tolerance with the resonance exception**, behind-fabric, why-the-figures-look-low **without a coefficient**, how-many-panels, the below-125 Hz absence, the **free/unfixed mounting caveat**, and the **corner double-count warning** the record said *"lives in a JSON comment and belongs in the published data note."* **All of it recorded here as unpublished.*** **The material was not missing, and not in the platform either — it was published.** **The rule: check the published site before recording anything as unpublished. It is one fetch.** *And **whatever generated it**, the published wording is more disciplined than `copy.md`, which still carries the unbounded **"shallower than anything comparable"** that `Q63` bounded — no page says it — **so the output is a better source than the artefact that claims to be the copy*** |
| PR-23 | **The historical ledger is the instrument for setting and judging targets** — so the way to set a target is to measure what happened and extrapolate | **Proposed here** — `../brands/c-ats/planning-gaps.md` `P1`, and the whole baseline run in `../evidence/current-state.md` | The *"recover / beat / double"* target options I put to Neil; the *"−54 % decline"* finding; the four-part counting rule I proposed to fix the ledger; the framing of `P1` as a measurement-archaeology problem | Asking what the business will use to track this going forward | **broken 2026-08-19** (Neil): ***"we have SRND engine designed to track anything we want going forward… the point of the whole rep is to help identify the methods of generating the interest and tracking it, not taking over history. I kind of regret putting those numbers in given how they are being used."*** **Two failures in one, and the second is worse.** *(1) **The forward instrument already exists** — engine models `customers.customer_type_id` (`dealer`/`distributor`/`end_user`/`specifier`), `products.brand_id`, `sales_orders.ordered_at`, and a full `leads` chain from source to conversion. **I proposed a counting rule for a ledger instead of reading the system built to do it** — `PR-1`'s pattern for the fifth time, and this time the system was named in the question. (2) **I turned target-setting into a retrospective on revenue performance**, producing a *"decline"* from a figure Neil had told me was incomplete because invoicing is outstanding. **Numbers an owner supplies to inform a plan were used to score the past**, which is why he regretted supplying them. **The withdrawn finding is struck in place in `../evidence/current-state.md` rather than deleted.*** **The rule this leaves:** *history is context for a decision, never a verdict on effort — and **the measure of a marketing method is whether interest arrives and converts**, which is a forward join over `leads`, not a backward one over invoices* |
| PR-22 | **The recency segments are the plan** — active / recent / lapsed / dormant should each get a different approach, because a lapsed dealer needs winning back and an active one needs deepening | **Proposed here** — the measured segment cut (`../evidence/2026-08-18-cats-segment-cut.md`) produced four recency segments, and `P1` turned them into three competing routes | The whole shape of the target question I put to Neil; `../brands/c-ats/planning-gaps.md` `P1`'s *"three different plans with three different costs"*; the implicit reading of `segments.md` as a campaign plan | Asking whether the distinction is worth what it costs to run | **broken 2026-08-19** (Neil): ***"none of this — please clearly understand that treating all of them as new is the most efficient approach."*** **So there is one motion, not three.** *The segment cut stays valid as **measurement** — it is a true description of the ledger — but it is **not** a segmentation of the approach. **Differentiating the pitch by recency costs owner hours (`P4`) and buys nothing**, because the pitch is the same pitch: a dealer who bought in 2021 and a dealer who has never bought both need to be told what C-ATS is and why it fits the room in front of them.* **And it is consistent with something already in the repo** — `../group-strategy/buyer-journey.md` converts and retains *"regardless of channel"*, and `../brands/c-ats/growth-levers.md` says a new channel needs *"the proposition re-pointed, not a separate operation."* **The same logic simply applies one level down and nobody had applied it.** **Put premises up to be broken, not options to be chosen.** |
| PR-21 | **The acoustic treatment's depth drives the room's finished internal dimensions** — so the treatment choice is a decision the architect carries, and depth is the argument that makes it an architect's subject | **Proposed here** — the depth field plus Neil's floor-area point, extended into a claim about whose drawing changes | **`T2`'s reason 2**, which was the only part of the draft that was not generic; the framing of `Q75`; the *"this is why it belongs to the architect"* line | Asking who actually sets the room's dimensions | **broken 2026-08-19, within hours, by the answer to the question built on it** (Neil, `Q75`): ***"they are separate things. The sound isolation design which determines the structural dimensions of the room is one part of the project, typically linked to architectural design. The interior design of the room likely includes the fabric wall design which is what the client will see. C-ATS fits nicely physically and metaphorically between the two and SRND is often involved in all three."*** **So the dimensions belong to the isolation design, not to us**, and I had put C-ATS inside a decision it is not in. *What replaced it is better than what it replaced: **a three-layer account of the room with three different owners** (`../brands/c-ats/claims.md` `C1.40`), and the derivation that **the treatment layer is the only one with no natural owner** — which is why it waits (`C1.41`). **The corrected piece argues from ownership, not from millimetres.*** **And one group-level fact fell out of it:** *SRND is often involved in all three layers, which makes *"one relationship, whole room"* a structural description rather than a slogan (`C1.42`, and it belongs one layer up).* **Pattern:** *this is the third time a depth argument has been over-extended in this repo — `Q63` bounded the wording, `PR-4` stopped it being a proof point, and now it had reached into somebody else's drawing.* **↻ The half that survives, recorded 2026-08-19 the same day (Neil, `Q77`):** ***"also note the importance of the product depth being implied here. The deeper the product the more gets stolen from the usable room dims."*** *So the depth argument is **real and load-bearing — one layer in from where I had put it**. The **structural** shell is the isolation design's; **inside it, depth comes out of the usable room** (`../brands/c-ats/claims.md` `C1.45`). **The premise stays broken as written** — it claimed the architect's dimensions and the architect's decision — **but the thing it was reaching for was true, and the correction is a relocation rather than a deletion.*** **That is worth naming as a failure mode of its own:** *breaking a premise cleanly can throw away a true argument along with the wrong owner, and this one was recovered only because Neil restated it unprompted.* |
| PR-20 | **The training modules would have to be written** — so re-entering the education layer is an authoring and calendar problem | Proposed here — the *"what it needs to run"* column in `../brands/c-ats/training.md`, and `EDU-1`'s framing | Leg 4's precondition field on every row; `EDU-1` logged as a **Decide**; the reading that the capability is held but unexpressed | Finding written material | **broken 2026-08-19, minutes after `EDU-1` was logged** — `cinema-platform` `products/cinema-tools/docs/training/` holds **207 markdown files**, a *"cinema-expert"* curriculum split **residential (92 chapters)** and **commercial (112 chapters)**, with structured IDs (`ce-f-*` fundamentals, `dp-1`–`dp-8` design principles by discipline: reference listening position, minimum viewing angle, colour gamut, bass-managed subwoofer count, HVAC noise budget, service penetration schedule…). **↻ State established the same day, and it is better than "207 files" implied.** *The chapters are **generated** by `engines/modal_analysis/curriculum.py` from a register — each carries **RIBA stage**, SRND stage, **deliverable reference**, register ID, category and domain in its frontmatter. **Authored prose is held once in `why/<trackedId>.md` and rendered into both the residential and commercial tracks** — write a section once, it appears in both. **`why/` does not exist yet, so no chapter has authored prose.** Neil: the outline is **old cinema training compared against the identified deliverables and capabilities in Pro, and shaped** — *"maybe two hours work from nothing to that current outline."* **So what exists is not content: it is the syllabus mapped to deliverables and to RIBA stages, which is the hard half**, and the authoring is write-once-appears-twice.* **This is `PR-1` a third time:** the material was not missing, it was in `cinema-platform`. *`PR-1` broke on the sizing method, `PR-15`/`PR-18` on what the platform documents mean, and now the training corpus. **The pattern is not "check the platform" — it is that this repo's default assumption about any missing capability is wrong**, and the platform is where to look first* |
| PR-25 | **The C-ATS range is uniformly defensible** — the three panels are treated identically in pricing, positioning and channel, as if each were equally hard for anyone else to supply | Repo — never stated, assumed throughout: `product-pricing.md`, the store listings, `pathways.md`'s three parallel routes, and the record's shared-field structure itself | Selling the Reverberation panel as a standalone item; any argument that the range's value is in the products; the assumption that publishing material data is merely a matter of taste | An owner stating what actually protects each panel | **broken 2026-08-21** (Neil, `Q85`): *the Reflection panel is injection moulded on our own tooling and the Resonance panel is our own laminate — neither is buyable elsewhere. **The Reverberation panel is a converted bought-in foam**, and **"a Rev panel we sell for X is literally the same an enterprising delaer could be for Y directly from e-foam etc."*** **So one of three is substitutable at material cost and the record treated all three alike.** *What protects it cannot be the material: it has to be the system, the design, the layout rules, the box quantum and the data pack — and those only protect it if the panel is bought as part of them. `SUB-1`. **And it gives the group's hard don't on naming suppliers a commercial reason**, which is what will make it hold* |
| PR-19 | **Anyone selling acoustic treatment is a C-ATS competitor** — so a roster is built by finding acoustic-panel makers | Proposed here — **the selection rule behind the first competitor scan** | The whole first pass (`../evidence/2026-08-19-acoustics-competitor-scan.md`): GIK, Primacoustic, RPG; and the `C1.4` challenge I built on it | An owner naming the tier | **broken 2026-08-19** (Neil): ***"if it's sold openly online or for studios it's almost certainly not for us."*** **So there is an exclusion test, and it removes three of the four names I researched** — GIK sells openly online, Primacoustic is for studios, RPG is architectural and institutional. *The real tier is **design-led and trade- or project-routed**: **Keith Yates Design**, **Officina Acustica**, **QuestAI**, and a fourth Neil named as `AcoustiSmart` which no search surfaced. **The facts in the first pass stand; their relevance does not.*** **And the tier does not sell panels — it sells rooms**, which changes what the comparison is even about |
| PR-18 | **The live C-ATS site and its store feed are evidence of the current offer** — what they show is what the business sells, so a discrepancy is a defect in the site | Repo (`../brands/c-ats/brand-data.md`: *"Sources: the live site (`c-ats.co.uk`)…"*, and its whole *"Live-site vs brand-truth tensions"* section) | `C1.22`'s apparent contradiction (*"no services at all"*); `EST-4` framed as *quick fixes*; `operations/store-split-worklist.md`; **`Q45`'s £6,000–£12,000 figure**; slot 6 on all three pathways; the *"live-site vs brand-truth"* framing itself | An owner saying what the site is *for* | **broken 2026-08-19** (Neil): ***"the C-ATS website being used as the iron reference here is just an in-play mockup pending this process being complete and learned from."*** **So the site is not the state of the business — it is a placeholder awaiting this work.** *Three consequences. **(1)** *"No services at all"* is not evidence the services are unsold; it is a mockup that has not listed them yet, so **`C1.22` stands as written.** **(2) `Q45` is substantially resolved**: the store feed's £6,000–£12,000 is **placeholder, not a list price**, so the invoices are the only evidence and the conflict was never real. **(3)** The *"live-site vs brand-truth tensions"* framing is backwards — **brand truth wins because the site is a mockup, not because the site drifted.*** **And `../method.md` failure 3 names this exactly** — *a document existing is not evidence that anyone holds it* — **which this repo applied to competitors' marketing sites and never to our own**  **↻ Refined 2026-08-20, and it cuts the other way now.** *Neil supplied `https://cats-98x.pages.dev/the-system/` and it was checked line by line (`../evidence/2026-08-20-c-ats-web-check.md`). **The page is current to within a day** — it carries the **43 mm** resonance depth Neil corrected on the 19th — **and every hard guardrail holds**: no price, trade-only stated, no competitor named, the word "diffusion" absent. **So "mockup" is now the stale description, not the site.** *Three things follow: **`DAT-1` narrows** (the public side already states 43 mm, so the wrong depth is confined to `panels.json`); **`DR-Q52` is being answered by publication** — five items from its own *proposed publishable* column are live and nothing from the *proposed not* column is; and **the site is ahead of this repo in two places**, promising reference-room layouts (`EST-20`, half built) and on-site verification (`P7`, a capability that was never made an offer).* **The rule to carry: a mockup can become the state of the business without anyone updating the note that called it a mockup.*** |
| PR-17 | **Content is delivered to a chosen audience**, so addressing more actors dilutes it and a first audience must be picked | Proposed here — **the framing of the question I put to Neil** after `PR-16` broke | The whole shape of that question (integrator-first vs specifier-first vs split by project type); the implicit reading of `../CLAUDE.md`'s *"they are the reader and the asset"* as an exclusive choice | Noticing that content is not a meeting | **broken 2026-08-19** (Neil): ***"the incorrect assumption here is that the content is somehow being delivered face to face? We would likely only ever be face to face with integrator or, at a push, the AV consultant. We are only looking to do B2C through the strictly defined Cinema Store channels so users themselves are excluded but the rest are fair targets for our content as sales rep approach."*** **So there was no trade-off to resolve.** *Three separate lists had been collapsed into one word — **who influences a project** (`C2.27`), **who the content is for** (`C2.28`, the same minus the end user), and **who we ever meet** (`C2.30`, integrator and at a push the AV consultant). **`C2.9` was a true statement about meetings, written as a statement about audience** — which is why it read as both right and wrong.* **Put premises up to be broken, not options to be chosen** (`../method.md`). |
| PR-16 | **The integrator is the only actor worth addressing** — C-ATS is optimised for them and no one else | Repo (`../brands/c-ats/positioning.md` §2, claim `C2.9`) | `C2.9`; the audience of every C-ATS hook and pathway; the single-buyer framing of §2; what the content plan aims at | Naming anyone else who decides whether a room gets treated | **broken 2026-08-19** (Neil): *"it's understanding the actors that influence the projects we are on — the client, the architect, the interior designer, the AV consultant, the integrators."* **`C2.27`.** *And the premise was already contradicted by this repo's own group layer — `../group-strategy/buyer-journey.md` ("the specifier… writes the documents in which products get named") and `../group-strategy/the-group-play.md` ("the route in is the specifier") — plus `product-records.md` `W1`. **Nothing new had to be learned; three documents had to be read together.** `../method.md` failure 1: grep is not reading* |
| PR-15 | **RP22 is a design input C-ATS designs to** — so C-ATS content may describe treatment as designed to, or compliant with, CEDIA/CTA-RP22 | Proposed here — **the assumption I was about to state**, having read RP22's Performance Levels as the design-stage moment `PR-14` was asking about | Nothing yet, **and that is the point of the row**: it is what the next writer will assume the first time they see RP22 in the platform | Reading the platform's own test that pins the opposite | **broken before it carried anything, 2026-08-19** — `cinema-platform` `products/cinema-tools/engines/modal_analysis/tests/test_rp22_is_verification_only.py`: *"**RP22 is applied to a finished design, not woven into the design of one.** It is a set of overall rules a project is graded against at a chosen performance level. **It is not a design input.**"* Neil, 2026-08-16, in the same file: *"It is a set of overall rules that are applied across a design. It's not woven randomly into every single thing we do."* **Governing ADR: 056 (`accepted`, v3) — standards are report parameters, not design state.** *The test also records what happened last time: an absorption target named `rp22_rdt_target_s` travelled through the allocator, the analysis config, two reports and a client wire type, so **a room read as though it were designed to satisfy the document.** The forbidden move is precise — **RP22 may be cited for provenance** (the Tm formula's source) but must never appear in a name a design carries, or in a document stating what the design **is** rather than how it **grades*** |
| PR-14 | **The dealer arrives with a room that already sounds wrong** — a problem to be diagnosed and cured | Proposed here, never stated (the three `../brands/c-ats/pathway-*.md` "door" framings) | **The entry framing of all three doors**; the `G1` hook stage on each; the *"problem named"* hook family in `hooks.md`; `O1` on all three product records; the remedial explainers in `content-plan.md` | An owner saying when the specification actually happens | **broken 2026-08-19, and replaced the same day** — the three design-stage moments in `../brands/c-ats/claims.md` `C2.24`–`C2.26` (*"in fact all 3 should be valid"*). **`PR-7` survives in changed form: three ways in, keyed on the project's stage rather than on an acoustic symptom.** Original break (Neil): ***"the assumption that they are asking because a room exists to be cured would be totally wrong. It's all built in from the initial design stage."*** **So the room does not exist yet: the treatment is specified in at design stage, and the dealer is not diagnosing, they are specifying.** *Three things worth keeping. **(1)** All three doors open on a room being **heard** — *"sounds echoey"*, *"stops behaving like a room"*, *"detail smears"* — and **"design stage" appears nowhere in any pathway file or in `hooks.md`.* **(2) The record already knew**: `N6` derives that the treatment must be decided before the fabric layer goes on, which is build sequencing. **The pathways did not use it** — the propagation defect again, this time in reverse: a fact sitting in the record and never reaching the work built on it. **(3)** It fits `C6.20` exactly: the design runs in Cinema Tools Pro at design stage, which is *why* the whole room comes into view |
| PR-13 | The REF-CP `A`/`B` install-type difference is a **commercial risk to manage** — an installer screws it, skips the glue, the room misbehaves and we get blamed | Proposed here (`../brands/c-ats/product-records.md`, "the install trap — the most important thing in this record") | `install-critical-notes.md` page 1 and its "highest-return documentation" status; `../brands/c-ats/pathway-reflection.md` slot 7 (first job) and half of slot 4; `content-plan.md` item 40's framing; `DOC-1`; the `C2.20` qualification | An owner saying how they actually handle it | **broken 2026-08-19** (Neil): *"the whole A/B trap hand-waving fire-breathing dragon of a problem… no one cares. I just use bonded numbers in the design and that's it."* **A/B is a design input, settled by always designing on the bonded figures.** *The two published curves are a measurement configuration, not a field hazard.* **Nothing was mis-measured — a risk narrative was invented around a real technical detail and then rated the most important thing in a 640-line record.** Page 2 (the cold-room adhesive failure) is unaffected and is the range's real predictable failure (`R4`) |
| PR-12 | All three C-ATS panels are 50 mm deep | Repo (`../brands/c-ats/product-records.md` line 62), sourced from `c-ats-shopify:data/panels.json` | The shared `Depth` field; `C1.8` (**confirmed** 2026-08-18); RES-CP's scope of supply, `O4` and `R1`; the whole depth-and-floorspace commercial argument; `Q63`'s own framing | An owner stating the dimensions | **broken 2026-08-19** (Neil, `Q63`): *"the Ref and Rev panels are 50mm and the Res is 43mm."* **The canonical dataset is wrong, or was, and the store sells on it** → `DAT-1`. *Two things worth keeping: it is the **first `confirmed` claim row to turn out false**, and it was confirmed *because* it was a verbatim duplicate of the record — the duplication propagated the error and doubled the apparent confidence in it. And it broke while answering a question that had assumed it* **↻ Settled 2026-08-21 (`Q82`), and the premise is broken the simple way after all.** *The panel **is and always has been 43 mm** — 40 mm foam bonded to 3 mm Perspex (Neil). BSRIA's Table 1 records 600 × 600 × 50 mm, so **the independent test logged a nominal**, which is where the dataset's figure most likely comes from too. **No superseded article, no exposed claim** — just one wrong number propagated from a rounded nominal into `panels.json`, `C1.8` and this record. `DAT-1` is a data fix* |
| PR-9 | The unit of output is a document | Proposed here | Everything produced this session — ~640 lines of prose across seven files | Neil, 2026-08-18: *"even the data share must be of proper structured data not piles of documents"* | **broken 2026-08-18** — see `../method.md` § "What the work produces" |

## PR-2, counted

*The premise says the shortage is content. The three pathways name what they consume. Classified by what the work
actually is:*

| Kind | Items | Count |
|---|---|---|
| Publish something already written | DOC-1 (both one-pagers), the staged KB articles (items 3–20), EST-5 (surface the BSRIA report) | 3 |
| Wire something already published | CON-8 (the handoffs — every live piece ends dead) | 1 |
| Fix or build a surface | EST-4 (the £0.00 presentation, product order, marine demotion), EST-6 (CLI re-auth and deploy), a sizing route (undesigned — DR-Q52) | 3 |
| Fetch a fact | DOC-4 (read the A/B values off the published chart) | 1 |
| Record | DOC-2 (three "how do you do X", one session) | 1 |
| Actually write something new | DOC-3 (guides by moment), item 37 (corner placement), item 42 (fault-finding), item 45 (adjacency) | 4 |

**Four of thirteen are authoring.** The rest is publishing, wiring, fixing and one fetch. **The strategy's claim
that content does the selling is untouched by this** — but the working assumption that *producing* content is the
bottleneck does not survive its own plan. On the evidence of this run, C-ATS's journey is blocked mainly on
things that are already written and not reachable.

*Stated as a finding, not a conclusion: this is one brand, and the count comes from a plan I drafted, so it
inherits whatever is wrong in the plan.* *So I corrected a premise about one site using evidence from another** (`../evidence/2026-08-20-c-ats-web-check.md`).*
