# The standards to set — a decision sheet

*The `[decide]` calls from `group/12-task-shapes.md`, collected into a run sheet. Pass 1 found the 19 `varies`
tasks fail on `no standard` — **nobody decided what good looks like** — so these are the decisions that turn the
templates into standards. Each is minutes. Once set, the answer replaces the `[decide]` in the matching shape,
and the task stops varying by whoever does it.*

## How to run it

Same method as the QA: **one at a time, accept the proposed default or override it.** A proposed default is a
**starting point, not a fact** — it is there so the fast answer is "yes, that" and only the ones you'd change
need thought. Where a decision needs your real business truth rather than a sensible guess, it is marked **⚑**
and the proposal is explicitly a draft to correct.

*When answered, each result lands back in `group/12-task-shapes.md`, replacing the `[decide]` in that task's
shape. Nothing here is settled until you say so.*

---

| # | The standard to set | Proposed default | Weigh / notes |
|---|---|---|---|
| 1 ⚑ | **Dealer bar** — what makes a dealer worth pursuing (`T-S03`) | Pursue if they build high-end rooms (the heartland) **or** the adjacent commercial/hospitality work, and aren't locked to a rival; hold if unproven; decline if no fit | **Your truth.** Is the bar build-type, brand-fit, or scale? This one sets the top of the funnel |
| 2 | **Project bar** — worth the time (`T-S04`) | Pursue if live (named room + timeline) and within served capability; watch if early/unconfirmed; pass if speculative | Cheap call; mostly confirms the obvious |
| 3 | **Presentation minimum set** — per room type (`T-S05`) | For a cinema room, always present the whole room-system spine — acoustics, screen/projection, fabric, audio — even on a single-brand enquiry | This *is* the cross-sell lever (`NEXT.md` lane 4). Alternative: lead with the enquiry brand, name the adjacent layer only |
| 4 | **Comparison dimensions** — the fixed axes every analysis uses (`T-M02`/`T-S23`) | Measured performance · integration/control · support model · lead time · price posture · reference/credibility | Refine per category from `06-competitors.md` (screens, motion, whole-build, design tools, distributors). The axes are what make analyses comparable |
| 5 ⚑ | **Which competitors, how often** (`T-M02`) | Track the named peers already in `06-competitors.md` — Screen Research, Artnovion, Cinema Build Systems, the StormAudio-class audio peers, and the distributor channels (Habitech et al.); quarterly + on any surface change | **Your truth** on the roster. Cadence is a safe default |
| 6 | **"Pattern" threshold** (`T-S23`) | Two or more losses to the same competitor on the same dimension in a rolling window = a pattern worth acting on | Simple, tunable later |
| 7 | **"Worth capturing" bar** (`T-S07`) | Capture into `N3` any answer that isn't already in the record **and** could recur (a general technical point) — not one-off project arithmetic | Keeps `N3` signal, not noise |
| 8 ⚑ | **`G2` — what each brand will/won't say** (`T-M03`) | Inherit each brand's documented hard don'ts (its brand-truth `CLAUDE.md`) plus the group rule: no superlative without a measurement | **Brand truth**, stays per brand (e.g. C-ATS: scattering never diffusion, no invented figures, no supplier names). Confirm nothing's missing |
| 9 | **Collateral re-review cadence** (`T-M10`) | Re-review on change (a product or data change triggers it) with an annual backstop | Event-driven beats calendar-driven |
| 10 | **Floor rate** — sustainable minimum publish cadence (`T-M06`) | A **weekly** floor, derived from what the studio already produces | `NEXT.md` lane 6: "a weekly floor met all year beats a fortnightly aspiration abandoned in March." The number is yours; weekly is the shape |
| 11 | **Channel-fit rules** (`T-M07`) | Publish to **brand** channels, not the group one; short argument-making pieces to video, method/data to the knowledge base; group channel = group-level only | `decided.md` S29; DT's channel carries ~¾ of the audience |
| 12 | **Follow-up interval + stop rule** (`T-S09`) | A touch roughly every 30 days through the quote-to-order window; hand to a person as the decision date nears or after N unanswered touches; stop at the order or an explicit no | The lead-time build, `TSK-3`. Interval matched to project length |
| 13 | **Post-event note minimum** (`T-S25`) | Per contact: who, which account, what they were unsure about, what they were afraid of, the next step | Mirrors the `T-S06` discovery capture (`NEXT.md` lane 4) |
| 14 | **Recognition milestones** (`T-S28`) | First order · a big-£ threshold (the £500k-dealer class) · an Nth-order or anniversary mark · a standout project | Signal-triggered, never a calendar (`decided.md` S28). Record side: `REC-11` |
| 15 | **Lead routing** (`T-M09`) | Explicit ask, a qualified high-value lead, or a live project → a person; everything else → the nurture sequence | Tunable once volumes are known |
| 16 | **Reporting period + headline signals** (`T-M15`) | Monthly; headline = the gateway signals `G1`–`G6`, plus the cross-brand-accounts number (`FACT-1`) | The fixed set is what makes periods comparable |

---

## Reading the result

Three of the sixteen carry the **⚑** — the dealer bar, the competitor roster, and each brand's `G2` — because
they need real business or brand truth and a guess would be worse than a gap. The other thirteen have safe
defaults; expect most to be a "yes, that." **The ⚑ three are the ones worth actual thought**, and the dealer bar
(1) is the highest-leverage of the lot — it sets the top of the whole funnel, and every qualification downstream
inherits it.

*When each is settled, it replaces the `[decide]` in `group/12-task-shapes.md`, and that task is now a standard,
not a habit.*
